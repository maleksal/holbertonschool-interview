#ifndef SORT_H
#define SORT_H

#include <stdlib.h>
#include <string.h>
#include <stdio.h>

void merge_sort(int *array, size_t size);
void print_array(const int *array, size_t size);

#endif /* SORT_H */
